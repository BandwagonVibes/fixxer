# PhotoSort v9.1 Dependencies
# Install with: pip install -r requirements.txt

# ============================================
# Core Dependencies
# ============================================
Pillow>=10.0.0              # Image processing
requests>=2.31.0            # API calls to Ollama
imagehash>=4.3.1            # Legacy burst detection (fallback)

# ============================================
# v8.0 Engines (Optional but Recommended)
# ============================================
# CLIP-based semantic burst detection
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# BRISQUE quality assessment
image-quality>=1.2.7        # Also known as 'imquality'
opencv-python>=4.8.0

# ============================================
# EXIF and Metadata
# ============================================
# Note: PhotoSort uses exiftool (external command)
# Install with: brew install exiftool (macOS)
#           or: apt-get install libimage-exiftool-perl (Linux)

# ============================================
# RAW Format Support
# ============================================
# Note: PhotoSort uses dcraw (external command)
# Install with: brew install dcraw (macOS)
#           or: apt-get install dcraw (Linux)

# ============================================
# UI/Progress (Optional)
# ============================================
tqdm>=4.66.0               # Progress bars
questionary>=2.0.0         # Interactive prompts

# ============================================
# Additional Utilities
# ============================================
numpy>=1.24.0              # Numerical operations

# ============================================
# Development Dependencies (Optional)
# ============================================
# Uncomment for development work:
# pytest>=7.4.0            # Testing
# black>=23.0.0            # Code formatting
# flake8>=6.1.0            # Linting
# mypy>=1.5.0              # Type checking

# ============================================
# Installation Notes
# ============================================
# 1. Create virtual environment:
#    python3 -m venv venv
#    source venv/bin/activate  (macOS/Linux)
#    venv\Scripts\activate     (Windows)
#
# 2. Install dependencies:
#    pip install -r requirements.txt
#
# 3. Install system dependencies:
#    macOS:
#      brew install exiftool dcraw
#
#    Linux:
#      sudo apt-get install libimage-exiftool-perl dcraw
#
# 4. First run downloads CLIP model (~600MB)
#    Stored in: ~/.cache/torch/sentence_transformers/
